---
import "./about.css";
---

<section id="nosotros"
    class="about w-full h-auto flex flex-col items-center overflow-hidden justify-center py-20"
    aria-labelledby="titleAboutUs"
>
    <h1 id="titleAboutUs" class="title-about text-grisClaro font-sans text-[96px] font-extrabold">
        Quiénes <span class="text-yellow">Somos</span>
    </h1>
    <p id="textAbout" class="text-about w-4/6 text-grisClaro text-[40px] font-bold text-center">
        En <strong>Maestría del Hierro</strong>, contamos con más de 30 años de experiencia perfeccionando la ornamentación y soldadura. 
        Combinamos tradición, innovación y excelencia para ofrecer soluciones personalizadas y duraderas que superan expectativas.
    </p>    
</section>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const title = document.getElementById("titleAboutUs");
        const text = document.getElementById("textAbout");
        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    title.classList.add("title-is-visible");
                    text.classList.add("text-is-visible");
                } else {
                    title.classList.remove("title-is-visible");
                    text.classList.remove("text-is-visible");
                }
            });
        }, { threshold: 0.40 });

        observer.observe(title);
        observer.observe(text);
    });
</script>
